"""Housing strand — no participant-trained forecast models.

The challenge uses a frozen **household-type mean** baseline whose outputs are
already in `data/raw/housing_with_forecasts.csv`. Teams audit data quality,
design splits, and benchmark fairness — they do **not** implement new trainable
forecast predictors in this repo.

If organisers add optional extensions, place them here.
"""
