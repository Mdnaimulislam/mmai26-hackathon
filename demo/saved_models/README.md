# Saved Models

Serialized demo baseline models (`*.joblib`) are generated examples.

Participants should retrain and replace these artifacts using their own expanded pipeline and full-data evaluations.
