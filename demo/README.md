# Clinical Strand Demo — Reference Walkthrough

This demo is a guided reference implementation for the clinical strand. Use it to understand the expected workflow, outputs, and evidence standards before preparing your team submission.

## Goal of this demo

- Run sample end-to-end clinical pipeline.
- Generate baseline model outputs and safety evidence artifacts.
- Use outputs to complete your own governance deliverables.
---

## Contents at a Glance

```
demo/
├── README.md               ← you are here
├── requirements.txt        ← Python dependencies (install these first)
├── app.py                  ← pipeline runner (generate_demo_outputs)
├── data/
│   ├── raw/
│   │   ├── tabular.csv             — 500 patients, one row per patient (feature matrix)
│   │   ├── notes.csv               — 344 clinical notes (subset of patients with notes)
│   │   ├── vitals_series.csv       — 12,000 rows (24 hourly vitals per patient)
│   │   └── README.md               — full feature guide for all three files
│   └── processed/          ← train/val/test splits — generated by notebook 01
├── models/
│   └── model_a.py          ← PyKale multimodal fusion (tabular + notes + vitals)
├── notebooks/
│   └── data_exploration_and_train.ipynb
├── saved_models/           ← model artefacts — generated by the notebook
├── src/
│   ├── evaluate.py         ← metrics and equity analysis utilities
│   └── report.py           ← JSON report generation utilities
└── reference/
    ├── omaib_pathway.json          ← submission template (item B)
    └── model_safety_report.json    ← submission template (item C)
```

---

## Prerequisites

- **Python 3.11 or higher** — check with `python --version`
- A Jupyter-capable IDE (see options below)
- A GitHub account (for submitting your own work)

---

## Step 1 — Set Up Your Environment

Using a virtual environment is strongly recommended to avoid package conflicts.

```bash
# Create and activate a virtual environment
python -m venv .venv

# Windows
.venv\Scripts\activate

# macOS / Linux
source .venv/bin/activate

# Install all dependencies (run from the demo/ directory)
pip install -r requirements.txt
```

`requirements.txt` installs:

| Package | Purpose |
|---|---|
| streamlit | Dashboard app |
| pandas, numpy | Data handling |
| scikit-learn | Preprocessing, metrics, TF-IDF |
| torch | Required by PyKale (Model A) |
| pykale | Multimodal neural network (Model A) |
| matplotlib, plotly | Visualisations |
| joblib, scipy | Model serialisation, stats |

> **Note:** `torch` and `pykale` are the heavier installs (~1 GB). Training Model A takes ~30–60 seconds on CPU.

---

## Step 2 — Open in Your IDE

### VS Code (recommended)

1. Open the `demo/` folder: **File → Open Folder**
2. Install the **Jupyter** extension (search "Jupyter" in the Extensions panel)
3. Open a notebook (`.ipynb`) — VS Code will prompt you to select a kernel; choose the `.venv` Python interpreter you created above
4. Run cells with **Shift + Enter** or click **Run All**

### JupyterLab

```bash
# Install JupyterLab if not already present
pip install jupyterlab

# Launch from the demo/ directory
jupyter lab
```

Navigate to `notebooks/` in the file browser and open each notebook in order.

### PyCharm Professional

1. Open the `demo/` directory as a project
2. Go to **Settings → Python Interpreter** and add the `.venv` interpreter
3. Open a `.ipynb` file — PyCharm will render it as a notebook
4. Run cells with **Shift + Enter**

---

## Step 3 — Run the Notebook (data_exploration_and_train.ipynb) from top to bottom

All data files are pre-included in `data/raw/` — no data download is needed.

### Data Exploration

**What it does:** profiles the synthetic dataset, identifies MNAR patterns, and highlights the clinical subgroups your models must handle correctly.

**What to look for:**
- Complication rates vary widely across surgery types (cardiac ~62%, orthopaedic ~6%)
- Note absence is concentrated in the highest-risk patients — MNAR, not random
- Vascular patients have suppressed heart rate and lactate due to beta-blockade — a model relying on vitals alone will miss them

**Key expected outputs:**

```
Shape: (500, 37)
Complication rate: 25.4% | Has notes: 68.8% | Blood loss MNAR: 33.8%

Note missingness:  cardiac 58% | vascular 42% | abdominal 21% | orthopaedic 14%
Blood loss MNAR:   cardiac 51% | vascular 45% | abdominal 24% | orthopaedic 24%

Vascular: HR mean = 70 bpm, ICU lactate = 0.95  (lowest of all surgery types)
```

**Produces:** no files — analysis only. Answer the reflection questions at the end before proceeding; they directly inform your deployment verdict in the app.

---

### Preprocessing & Split Section
**What it does:** applies median imputation to numeric features (excluding MNAR columns), then creates stratified 70/15/15 train/val/test splits preserving the complication rate.

**Key expected outputs:**

```
No NaN in numeric feature columns — no imputation needed.
Remaining NaN (excl. MNAR columns): 0

train : n= 350  complication=25.4%  notes=70.3%
val   : n=  75  complication=25.3%  notes=72.0%
test  : n=  75  complication=25.3%  notes=58.7%
```

**Produces:** `data/processed/train.csv`, `val.csv`, `test.csv`

---

### Model Training

**What it does:** trains Model A (PyKale multimodal fusion) on the train split, evaluates on val, checks calibration, and saves artefacts. Training takes ~30–60 seconds on CPU.

**Model A — PyKale Multimodal Fusion:**

| Branch | Input | Architecture |
|---|---|---|
| Tabular | Surgical context + ICU severity (12 features) | FCNet encoder |
| Text | TF-IDF on clinical notes (200 features) | FCNet encoder |
| Time series | Slope/variability from hourly vitals (18 features) | FCNet encoder |

**Key limitation:** when notes are absent (MNAR ~32%), the text branch receives a zero vector, causing systematic underestimation of risk for note-absent patients — concentrated in cardiac/vascular cases.

**Key expected outputs:**

```
Model A | Val AUROC=0.83–0.89

MNAR preview:
  With notes:    AUROC=0.85–0.90
  Without notes: AUROC=0.70–0.80
  Gap ≈ 0.05–0.15  >>> MNAR signal detected
```

**Produces:** `saved_models/model_a.joblib`

---

## Step 4 — Launch the App

```bash
streamlit run app.py
```

If you only need CLI metrics output without the Streamlit UI:

```bash
python app.py
```

## What artifacts should exist after a successful run

- `saved_models/model_a.joblib`
- `data/processed/train.csv`, `val.csv`, `test.csv`

## Data and model context

- Tabular, notes, and vitals modalities are used.
- This demo emphasizes clinically meaningful failure checks, especially:
  - Missing Not At Random (MNAR) note availability
  - MNAR blood-loss documentation effects

## What this demo does not do for teams
This demo does not auto-complete final submission narratives or verdicts.
Teams must provide their own evidence-backed interpretation.

Complete the templates in:

- `reference/omaib_pathway.json`
- `reference/model_safety_report.json`

## Troubleshooting

**`FileNotFoundError: train.csv not found`**
Rerun the notebook `data_exploration_and_train.ipynb` top to bottom to generate the processed splits.

**`ImportError: No module named 'kale'`**
PyKale was not installed. Run `pip install pykale>=0.2.0`. Also ensure `torch>=2.0` is installed first, as PyKale depends on PyTorch.

**Kernel not found in VS Code**
Click the kernel indicator in the top-right of the notebook, then select **Python Environments** and choose the `.venv` you created.

**`streamlit: command not found`**
Activate your virtual environment first (`.venv\Scripts\activate` on Windows, `source .venv/bin/activate` on macOS/Linux), then run `streamlit run app.py`.

**App shows "FileNotFoundError" on startup**
The app needs `data/processed/train.csv`, `test.csv`, and `saved_models/model_a.joblib`. Run `notebooks/data_exploration_and_train.ipynb` top to bottom — it generates the processed splits and saves the trained model.
