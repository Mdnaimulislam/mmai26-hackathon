# Clinical Strand Demo — Reference Implementation

This is a reference implementation of one possible solution to the clinical strand challenge. It shows what a completed solution can look like — not a template you must follow.

**Challenge concept:** Post-surgical Step-down Monitoring Brief — a handover tool that generates a personalised ward monitoring protocol for each patient based on AI-predicted 30-day complication risk.

---

## Contents at a Glance

```
demo/
├── README.md               ← you are here
├── requirements.txt        ← Python dependencies (install these first)
├── app.py                  ← Streamlit dashboard (run after the notebooks)
├── data/
│   ├── raw/
│   │   ├── tabular.csv             — 500 patients, one row per patient (feature matrix)
│   │   ├── notes.csv               — 344 clinical notes (subset of patients with notes)
│   │   ├── vitals_series.csv       — 12,000 rows (24 hourly vitals per patient)
│   │   └── README.md               — full feature guide for all three files
│   └── processed/          ← train/val/test splits — generated by notebook 01
├── models/
│   ├── model_a.py          ← LightGBM classifier (all tabular features)
│   └── model_c.py          ← PyKale multimodal fusion (tabular + notes + vitals)
├── notebooks/
│   ├── 00_data_explore.ipynb
│   ├── 01_preprocess_and_split.ipynb
│   └── 02_train_models.ipynb
├── saved_models/           ← model artefacts — generated by notebook 02
├── saved_metrics/          ← evaluation CSVs — generated by notebook 02
├── src/
│   ├── evaluate.py         ← metrics and equity analysis utilities
│   └── report.py           ← JSON report generation
└── reference/
    ├── omaib_pathway.json          ← submission template (item B)
    └── model_safety_report.json    ← submission template (item C)
```

---

## Prerequisites

- **Python 3.11 or higher** — check with `python --version`
- A Jupyter-capable IDE (see options below)
- A GitHub account (for submitting your own work)

---

## Step 1 — Set Up Your Environment

Using a virtual environment is strongly recommended to avoid package conflicts.

```bash
# Create and activate a virtual environment
python -m venv .venv

# Windows
.venv\Scripts\activate

# macOS / Linux
source .venv/bin/activate

# Install all dependencies (run from the demo/ directory)
pip install -r requirements.txt
```

`requirements.txt` installs:

| Package | Purpose |
|---|---|
| streamlit | Dashboard app |
| pandas, numpy | Data handling |
| scikit-learn | Preprocessing, metrics, TF-IDF |
| lightgbm | Model A training |
| torch | Required by PyKale (Model C) |
| pykale | Multimodal neural network (Model C) |
| matplotlib, plotly | Visualisations |
| joblib, scipy | Model serialisation, stats |

> **Note:** `torch` and `pykale` are the heavier installs (~1 GB). If you only want to run the exploration notebook and are not training Model C, you can skip pykale.

---

## Step 2 — Open in Your IDE

### VS Code (recommended)

1. Open the `demo/` folder: **File → Open Folder**
2. Install the **Jupyter** extension (search "Jupyter" in the Extensions panel)
3. Open a notebook (`.ipynb`) — VS Code will prompt you to select a kernel; choose the `.venv` Python interpreter you created above
4. Run cells with **Shift + Enter** or click **Run All**

### JupyterLab

```bash
# Install JupyterLab if not already present
pip install jupyterlab

# Launch from the demo/ directory
jupyter lab
```

Navigate to `notebooks/` in the file browser and open each notebook in order.

### PyCharm Professional

1. Open the `demo/` directory as a project
2. Go to **Settings → Python Interpreter** and add the `.venv` interpreter
3. Open a `.ipynb` file — PyCharm will render it as a notebook
4. Run cells with **Shift + Enter**

---

## Step 3 — Run the Notebooks in Order

All data files are pre-included in `data/raw/` — no data download is needed. Run the three notebooks **in sequence**; each notebook writes outputs that the next one reads.

### Notebook 00 — Data Exploration (`00_data_explore.ipynb`)

**What it does:** profiles the synthetic dataset, identifies MNAR patterns, and highlights the clinical subgroups your models must handle correctly.

**What to look for:**
- Complication rates vary widely across surgery types (cardiac ~62%, orthopaedic ~6%)
- Note absence is concentrated in the highest-risk patients — MNAR, not random
- Vascular patients have suppressed heart rate and lactate due to beta-blockade — a model relying on vitals alone will miss them

**Key expected outputs:**

```
Shape: (500, 37)
Complication rate: 25.4% | Has notes: 68.8% | Blood loss MNAR: 33.8%

Note missingness:  cardiac 58% | vascular 42% | abdominal 21% | orthopaedic 14%
Blood loss MNAR:   cardiac 51% | vascular 45% | abdominal 24% | orthopaedic 24%

Vascular: HR mean = 70 bpm, ICU lactate = 0.95  (lowest of all surgery types)
```

**Produces:** no files — analysis only. Answer the reflection questions at the end before proceeding; they directly inform your deployment verdict in the app.

---

### Notebook 01 — Preprocessing & Split (`01_preprocess_and_split.ipynb`)

**What it does:** applies median imputation to numeric features (excluding MNAR columns), then creates stratified 70/15/15 train/val/test splits preserving the complication rate.

**Key expected outputs:**

```
No NaN in numeric feature columns — no imputation needed.
Remaining NaN (excl. MNAR columns): 0

train : n= 350  complication=25.4%  notes=70.3%
val   : n=  75  complication=25.3%  notes=72.0%
test  : n=  75  complication=25.3%  notes=58.7%
```

**Produces:** `data/processed/train.csv`, `val.csv`, `test.csv`

---

### Notebook 02 — Model Training (`02_train_models.ipynb`)

**What it does:** trains two models on the train split, evaluates on val, checks calibration, and saves artefacts. Training Model C takes ~30–60 seconds on CPU.

**The two models:**

| | Model A | Model C |
|---|---|---|
| Architecture | LightGBM | PyKale 3-branch fusion |
| Modalities | All tabular features | Tabular + notes text + hourly vitals |
| Key limitation | Mean imputation underestimates cardiac/vascular blood loss | Zero-vector fallback for note-absent patients degrades MNAR subgroup |

**Key expected outputs:**

```
Model A | Val AUROC=0.82–0.87
Model C | Val AUROC=0.83–0.89  (better on note-present patients)

MNAR preview:
  With notes:    AUROC=0.85–0.90
  Without notes: AUROC=0.70–0.80
  Gap ≈ 0.05–0.15  >>> MNAR signal detected
```

**Produces:** `saved_models/model_a.joblib`, `saved_models/model_c.joblib`,
`saved_metrics/training_metrics.csv`

---

## Step 4 — Launch the App

```bash
# Run from the demo/ directory (not from notebooks/)
streamlit run app.py
```

The dashboard opens at **http://localhost:8501** in your browser.

### Dashboard tabs

| Tab | What to examine |
|---|---|
| 📋 Step-Down Brief | Per-patient risk scores, Model A vs C consensus, monitoring protocol |
| 📊 Model Performance | AUROC curves, calibration, ward simulation (per 100 patients) |
| ⚖️ Equity & MNAR | Surgery-type AUROC gaps; notes-absent AUROC gap for Model C |
| 🔍 Failure Modes | False negatives (missed complications) with patient details |
| 🏥 Deployment Assessment | Five deployment questions; structured governance checklist |
| 📝 Report Builder | Team info, verdict selection, JSON export (items B and C) |

**Threshold slider (sidebar):** default 0.35. Lowering it increases sensitivity (catches more complications) at the cost of more false positives. The ward simulation updates live.

### What the app exports

Clicking **Export** in the Report Builder tab writes two files to `reference/`:

- `omaib_pathway.json` — OMAIB Pathway Manifest (submission item B)
- `model_safety_report.json` — Model Safety Report (submission item C)

These are your structured evidence deliverables. Review them before submitting your PR.

---

## Data Guide

| File | Rows | Description |
|---|---|---|
| `tabular.csv` | 500 | One row per patient. Feature matrix used by the notebooks and models. |
| `notes.csv` | 344 | One row per patient with a clinical note. MNAR — absent patients are higher-risk. |
| `vitals_series.csv` | 12,000 | 24 hourly rows per patient (heart rate, RR, SpO₂, SBP, temp, lactate). |

**MNAR columns in `tabular.csv`:**
- `note_risk_score` — NLP-derived risk score; NaN when no note was written (~31% missing, concentrated in cardiac/vascular)
- `blood_loss_ml` — intra-operative blood loss; NaN when not recorded (~34% missing, concentrated in cardiac/vascular)
- `blood_loss_imputed` — mean-filled version; used by Model A but underestimates complex cases
- `blood_loss_missing` — binary flag (1 = MNAR); Model A uses this alongside the imputed value

See `data/raw/README.md` for the full column-by-column feature guide including value ranges, MNAR patterns, and model usage notes.

**This is synthetic data.** All patient identifiers and clinical values are generated. Do not use this data for your own submission — use the strand dataset provided.

---

## Troubleshooting

**`FileNotFoundError: train.csv not found`**
Run `01_preprocess_and_split.ipynb` first to generate the processed splits.

**`ImportError: No module named 'kale'`**
PyKale was not installed. Run `pip install pykale>=0.2.0`. Also ensure `torch>=2.0` is installed first, as PyKale depends on PyTorch.

**`ModuleNotFoundError: No module named 'lightgbm'`**
Run `pip install -r requirements.txt` from the `demo/` directory.

**Kernel not found in VS Code**
Click the kernel indicator in the top-right of the notebook, then select **Python Environments** and choose the `.venv` you created.

**`streamlit: command not found`**
Activate your virtual environment first (`.venv\Scripts\activate` on Windows, `source .venv/bin/activate` on macOS/Linux), then run `streamlit run app.py`.

**App shows "Run notebooks first" warning**
The app needs `data/processed/test.csv` and `saved_models/model_a.joblib`. Run notebooks 01 and 02 in order to generate these files.
